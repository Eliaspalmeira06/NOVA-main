version: '3.8'

services:
  # Servicio de la aplicación Node.js / Express
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - PORT=3000
    restart: always